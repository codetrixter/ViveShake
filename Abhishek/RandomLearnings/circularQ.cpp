struct circularQueue
{
    int data = 0;
    circularQueue* next = NULL;
};

void enQ(circularQueue& head, int item)
{
    while(head.next != NULL)
    {
        
    }
}
int main(int argc, char const *argv[])
{
    circularQueue* head = new circularQueue();
    head = NULL;


    return 0;
}
